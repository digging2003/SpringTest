<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.digging.spring.mvc.repository.RealtorRepository">
 	<insert id="insertRealtorByObject"
 	parameterType="com.digging.spring.mvc.domain.Realtor"
 	useGeneratedKeys="true" keyProperty="id">
	INSERT INTO `realtor`
	(
		`office`
		, `phoneNumber`
		, `address`
		, `grade`
		, `createdAt`
		, `updatedAt`
    )
    VALUES
    (
		#{office}
        , #{phoneNumber}
        , #{address}
        , #{grade}
        , NOW()
        , NOW()
    )
 	</insert>
 </mapper>